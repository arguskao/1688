name = "quote-list-system"
compatibility_date = "2024-01-01"

[build]
command = "pnpm run build"

# R2 Bucket binding (configure after creating bucket)
# [[r2_buckets]]
# binding = "R2"
# bucket_name = "product-images"

[vars]
# BUSINESS_EMAIL will be set via environment variables
# DATABASE_URL will be set via secrets (wrangler secret put DATABASE_URL)

[env.production]
vars = { RATE_LIMIT_PER_MINUTE = "10" }
