# =============================================================================
# Quote List System - Environment Variables
# =============================================================================
# Copy this file to .env for local development
# For production, use Cloudflare secrets: wrangler pages secret put <KEY>
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (Required)
# -----------------------------------------------------------------------------
# Neon PostgreSQL connection string
# Get this from: https://console.neon.tech
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# -----------------------------------------------------------------------------
# Email Service Configuration (Required)
# -----------------------------------------------------------------------------
# Email API key from your email service provider
# Supported providers: Resend, SendGrid, Mailgun
# Get Resend API key from: https://resend.com/api-keys
EMAIL_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Business email address to receive quote notifications
BUSINESS_EMAIL=business@example.com

# -----------------------------------------------------------------------------
# R2 Configuration (Optional - for image storage)
# -----------------------------------------------------------------------------
# R2 bucket name (configured in wrangler.toml)
R2_BUCKET_NAME=product-images

# Public URL for R2 bucket (if using custom domain)
# Leave empty to use API endpoint: /api/images/
R2_PUBLIC_URL=

# -----------------------------------------------------------------------------
# Rate Limiting Configuration (Optional)
# -----------------------------------------------------------------------------
# Maximum requests per minute per IP address
# Default: 10
RATE_LIMIT_PER_MINUTE=10

# -----------------------------------------------------------------------------
# Admin System Configuration (Required for admin features)
# -----------------------------------------------------------------------------
# Admin password hash (SHA-256)
# Generate with: echo -n "your_password" | shasum -a 256
# Or use the hash utility in the admin setup guide
ADMIN_PASSWORD_HASH=your_sha256_password_hash_here

# Session secret for signing session tokens
# Generate with: openssl rand -hex 32
SESSION_SECRET=your_random_session_secret_here

# -----------------------------------------------------------------------------
# Development Configuration (Optional)
# -----------------------------------------------------------------------------
# Set to 'development' for local development
NODE_ENV=development
